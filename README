Docker container for serving up data from processed videos

#upload a face of interest
curl -i -X POST -H "Content-Type: multipart/form-data" -F "data=@static/face.jpg" http://localhost:5000/api/1.0/makevector

if a face is found

#search for others from an image




